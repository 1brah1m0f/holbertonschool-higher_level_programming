#!/usr/bin/python3
def multiple_returns(sentence,length):
    if sentence == "":
        first = None
        lenght = len(sentence)
    else:
        lenght = len(sentence)
        first = sentence[0]
    return lenght
    return first